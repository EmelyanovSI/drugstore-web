export enum GroupBy {
    All,
    Country,
    Similar,
    Favorite
}

export enum Status {
    Idle,
    Loading,
    Succeeded,
    Failed
}

export enum ThemeMode {
    Light = 'light',
    Dark = 'dark'
}
